apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: taskmanager
    tier: frontend
spec:
  selector:
    app: taskmanager
    tier: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort

